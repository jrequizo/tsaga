
/**
 * Creates a minimally exposed caller for the Saga that can be safely passed and used within Sagas
 * 
 * TODO: we need to pass in the SagaRouter and SagaBuilder
 */
export default function createSagaCaller() {

}